<!DOCTYPE html>
<html lang="en">
<head>
    <script src="https://kit.fontawesome.com/2ca6785a00.js" crossorigin="anonymous"></script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - AwesomeSource</title>
    <link rel="stylesheet" href="/all.css" />
</head>
<body class="triangle">
    <div class="content">
        <h1 class="header">Login</h1>
        <% if (messages.error) { %>
            <h3 class="warning"><%= messages.error %></h3>
        <% } %>
        <form action="/login" method="POST">
            <div class="email">
                <input type="text" id="email" name="email" required class="form-email" placeholder="email" autocomplete="off">
            </div>
            <div class="password">
                <input type="password" id="password" name="password" required class="form-password" placeholder="password" autocomplete="off">
            </div>
            <button type="submit" class="login">Login</button>
        </form>
    
        <div class="register-text">
            <a href="/register" class="login-page" style="white-space: pre;">Don't have an account? Sign up.  <i class="fas fa-arrow-right"></i></a>
        </div>

    </div>
</body>
</html>